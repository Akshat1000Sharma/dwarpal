# Public origin of the dashboard. Only used to make social preview URLs absolute.
SITE_URL=http://localhost:3000

BACKEND_ORIGIN=http://localhost:8000

# Must match MERCHANT_API_TOKEN in backend/.env. Sent as X-Merchant-Token.
MERCHANT_API_TOKEN=replace-with-a-long-random-value
